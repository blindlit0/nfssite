"use client"

"use client"

// FeaturedEventHero removed — kept as a harmless stub to avoid breaking imports while cleaning up.
export default function FeaturedEventHero(): null {
  if (typeof window !== 'undefined') console.warn('FeaturedEventHero component removed (stub).')
  return null
}
