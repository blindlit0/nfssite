"use client"

// RecentPostsPreview removed — kept as a harmless stub to avoid breaking imports while cleaning up.
export default function RecentPostsPreview(): null {
  if (typeof window !== 'undefined') console.warn('RecentPostsPreview component removed (stub).')
  return null
}
