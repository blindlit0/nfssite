"use client"

// FeaturedEventsStrip removed — kept as a harmless stub to avoid breaking imports while cleaning up.
export default function FeaturedEventsStrip(): null {
  if (typeof window !== 'undefined') console.warn('FeaturedEventsStrip component removed (stub).')
  return null
}
